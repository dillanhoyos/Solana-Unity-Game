using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NativeCameraNamespace;

public class PhotoButtonLogic : MonoBehaviour
{
    // Start is called before the first frame update
    void StartCamera()
    {
        //NativeCameraNamespace.CameraCallback c;
        //NativeCamera.TakePicture(CameraCallback callback, int maxSize = -1, PreferredCamera preferredCamera = PreferredCamera.Default)
    }
}
